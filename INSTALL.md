# xAI CMS 安装教程

本文档将指导您完成 xAI CMS 的安装和部署。

## 1. 环境要求

在开始安装之前，请确保您的服务器满足以下最低要求：

*   **操作系统**: Linux (推荐 Ubuntu / CentOS) 或 Windows
*   **Web 服务器**: Nginx (推荐) 或 Apache
*   **PHP 版本**: PHP 8.0 或更高版本
*   **数据库**: MySQL 5.7 / 8.0 或 MariaDB 10.3+
*   **PHP 扩展**:
    *   `pdo_mysql`
    *   `curl`
    *   `mbstring`
    *   `json`
    *   `xml`
    *   `gd` 或 `imagick`

## 2. 文件上传与目录权限

1.  **上传文件**:
    将 `php_backend` 目录下的所有文件上传到您的服务器网站根目录（例如 `/www/wwwroot/your-domain.com`）。

2.  **设置目录结构**:
    建议将 Web 服务器的运行目录（Document Root）指向 `public` 文件夹，而不是项目根目录，以提高安全性。
    *   项目根目录: `/path/to/project`
    *   Web 根目录: `/path/to/project/public`

3.  **设置权限**:
    确保 Web 服务器用户（如 `www-data` 或 `www`）对项目目录拥有写入权限，以便安装程序生成配置文件。
    ```bash
    # Linux 示例命令
    chown -R www:www /www/wwwroot/your-domain.com
    chmod -R 755 /www/wwwroot/your-domain.com
    ```

## 3. Web 服务器与伪静态设置

在运行安装程序之前，您必须配置好 Web 服务器的伪静态规则，否则页面将无法正常访问（出现 404 错误）。

*   **详细规则请参考同目录下的 `REWRITE.md` 或 `伪静态规则.md` 文件。**

简要说明（宝塔面板）：
1.  进入宝塔面板 -> 网站设置 -> **网站目录**，将运行目录设置为 `/public`。
2.  **重要**：如果设置运行目录后遇到 `open_basedir` 报错，请在该界面取消勾选 **“防跨站攻击(open_basedir)”**，或者手动修改 `.user.ini` 将路径允许至项目根目录。
3.  进入 **伪静态**，选择 `ThinkPHP` 或手动填入 Nginx 规则（见 REWRITE.md）。

## 4. 运行安装向导

1.  打开浏览器，访问您的域名（例如 `http://your-domain.com`）。
2.  系统会自动检测到未安装，并跳转至安装向导页面 (`/install/index.php`)。
3.  **第一步：环境检测**
    系统会自动检查服务器环境是否满足要求。如果所有项均为绿色，点击“下一步”。
4.  **第二步：数据库配置**
    填写您的 MySQL 数据库信息：
    *   数据库主机: 通常为 `127.0.0.1` 或 `localhost`
    *   端口: 默认 `3306`
    *   数据库名: 您创建的空数据库名称
    *   用户名: 数据库用户名
    *   密码: 数据库密码
    点击“测试连接并继续”。
5.  **第三步：管理员设置**
    设置后台管理员账号：
    *   管理员邮箱: 用于登录后台
    *   管理员密码: 设置一个安全的密码
    点击“开始安装”。
6.  **第四步：完成安装**
    等待安装脚本执行完成（创建数据表、生成配置文件）。
    安装成功后，点击“进入首页”。

## 5. 安装后安全建议

1.  **删除安装目录**:
    为了安全起见，安装完成后请务必删除 `public/install` 目录。
    
2.  **配置定时任务 (可选)**:
    如果需要自动发布文章或生成 Sitemap，建议配置系统定时任务。

3.  **后台登录**:
    默认后台登录地址为：`http://your-domain.com/login`
